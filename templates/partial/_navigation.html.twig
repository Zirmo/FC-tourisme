<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_accueil') }}"><i class="fa-light fa-earth-americas"></i> FC-TOURISME</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mb-2 mb-lg-0 me-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#"><i class="bi bi-house-door"></i> Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_etablissements') }}"><i class="bi bi-building"></i> Etablissements</a>
                </li>

            </ul>
            <ul class="navbar-nav mb-2 mb-lg-0">

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% if app.user.pseudo %}
                        <span class="navbar-text text-white">{{ app.user.pseudo }}</span>
                    {% else %}
                        <span class="navbar-text text-white">{{ app.user.email }}</span>
                    {% endif %}

                <li class="nav-item">
                    <a class="btn btn-primary" href="/logout" role="button">Deconnexion</a>
                </li>

            {% else %}
                <li class="nav-item">
                    <a class="btn btn-primary" aria-current="page" href="{{ path('app_login') }}">Connexion</a>
                </li>
            {% endif %}

            </ul>

        </div>
    </div>
</nav>